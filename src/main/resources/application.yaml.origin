spring:
  application:
    name:
    microservice-customer:
      url:

  devtools:
    restart:
      enabled: true

  data:
    mongodb:
      uri:
      database:

server:
  port: 8080
  servlet:
    context-path: "/customer"
